am-navbar

am-spinner(*ngIf="isLoading")
.container.role-details(*ngIf="!isLoading && role")
  h2 {{ role.name }}
  .buttons.text-right
    a.btn.btn-light(routerLink="/roles") Cancel
    a.btn.btn-primary(routerLink="/roles/{{ role.id }}/edit") Edit
