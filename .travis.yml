before_script:
  - npm install -g webpack
  - npm install -g gulp
  - npm install

script:
  - npm build
  - npm test

language: node_js
node_js:
  - "node"

notifications:
  email:
    on_success: never # default: change
    on_failure: always # default: always
